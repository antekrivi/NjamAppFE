import { SortRestoraniPipe } from './sort-restorani.pipe';

describe('SortRestoraniPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRestoraniPipe();
    expect(pipe).toBeTruthy();
  });
});
